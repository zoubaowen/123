// placeholder — 这个文件仅用于验证 vitest 配置正确
// 真正的测试在 buildHistoryContextPrompt.test.ts 和 resolveOnPath.test.ts
import { describe, it, expect } from 'vitest'

describe('vitest baseline', () => {
  it('works', () => {
    expect(1 + 1).toBe(2)
  })
})
