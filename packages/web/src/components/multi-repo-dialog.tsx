// Stub: MultiRepoDialog component
interface MultiRepoDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function MultiRepoDialog({ open, onOpenChange }: MultiRepoDialogProps) {
  return null
}
