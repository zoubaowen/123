# 沙箱功能回归清单

重构沙箱实现后，对照此清单验证所有功能不遗漏。

---

## 一、Task 生命周期

| #   | 场景               | 验证内容                                     |
| --- | ------------------ | -------------------------------------------- |
| 1.1 | 创建 task          | 自动启动沙箱，agent 可操作文件               |
| 1.2 | 同 task 多轮对话   | 复用同一沙箱，之前的文件还在                 |
| 1.3 | 沙箱启动超时       | agent 不阻塞，降级为纯文字对话，用户收到提示 |
| 1.4 | 删除 task          | 沙箱文件/进程/归档全部清理                   |
| 1.5 | 沙箱崩溃后再次对话 | 自动重建沙箱，不报错                         |

---

## 二、工作空间

| #   | 场景                  | 验证内容                                                                                                                                                                                                     |
| --- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2.1 | isolated 模式         | 每个 task 独立文件系统，互不可见                                                                                                                                                                             |
| 2.2 | shared 模式           | 同用户 task 共享环境，通过目录区分 - **之前主要是为了省云函数资源，以环境 id 为 sessionid， task 开子目录，优先级低**                                                                                        |
| 2.3 | 指定工作目录          | agent 在正确的 CWD 下操作文件，**之前限制了 /tmp/workspace/xxx 为了协同 agent 和 沙箱，这里如果 ags 不在 /tmp/ 下可以不一定遵守，但是要是一个相对安全的目录，因为 codebuddy 需要在本地也建立出相同的目录才** |
| 2.4 | 从用户 Git 仓库初始化 | 创建 task 指定仓库 → 自动 clone → agent 基于现有代码工作                                                                                                                                                     |
| 2.5 | 文件跨会话持久        | 关闭页面 → 重新打开 → 之前写的文件还在                                                                                                                                                                       |

---

## 三、代码读写

| #   | 场景               | 验证内容                                 |
| --- | ------------------ | ---------------------------------------- |
| 3.1 | 读文件             | agent "读一下 package.json" → 返回内容   |
| 3.2 | 创建/写入文件      | agent "创建 hello.ts" → 文件出现在工作区 |
| 3.3 | 编辑已有文件       | agent "把端口改成 3001" → 精确替换       |
| 3.4 | 按名称搜索文件     | agent "找所有 .tsx 文件" → 返回路径列表  |
| 3.5 | 按内容搜索         | agent "哪里用了 useState" → 返回匹配位置 |
| 3.6 | 文件浏览器查看目录 | 侧边栏展开文件树 → 显示沙箱中的文件      |
| 3.7 | 编辑器手动编辑     | 点击文件 → 编辑 → 保存 → 沙箱文件更新    |
| 3.8 | 下载单文件         | 右键 → 下载 → 浏览器收到文件             |
| 3.9 | 下载文件夹         | 右键文件夹 → 下载 → 收到 zip             |

---

## 四、命令执行

| #   | 场景           | 验证内容                                     |
| --- | -------------- | -------------------------------------------- |
| 4.1 | 前台命令       | "运行 npm install" → 看到安装输出 + 退出码   |
| 4.2 | 后台命令       | "启动 dev server" → 不阻塞对话，进程后台运行 |
| 4.3 | 查看后台输出   | "看 dev server 日志" → 返回最新输出          |
| 4.4 | 终止后台进程   | "停掉 dev server" → 进程被杀                 |
| 4.5 | 命令超时       | 死循环命令 → 超时自动终止                    |
| 4.6 | 进程跨重连存活 | 沙箱重连后能发现仍在运行的进程               |

---

## 五、实时预览

| #    | 场景                     | 验证内容                                                                                                                                                                                                    |
| ---- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 5.1  | coding 模式拉起沙箱      | 选择 coding 模式创建 task → 沙箱自动启动                                                                                                                                                                    |
| 5.2  | 自动初始化项目模板       | 空工作区 → 自动 scaffold（vite + react）                                                                                                                                                                    |
| 5.3  | 自动安装依赖             | scaffold 完成 → npm install 自动执行（之前是通过直接解压打包好的 node_modules 完成的，防止安装太慢。 没选用直接同步是因为 之前推送这么多小文件太慢了，所以打了个 压缩包， 然后原始文件 gitignore 了没归档） |
| 5.4  | 自动启动 Vite dev server | 依赖安装后 → npm run dev 自动启动 → 端口监听                                                                                                                                                                |
| 5.5  | dev server 就绪检测      | 轮询状态 → 返回 vitePort + viteReady=true                                                                                                                                                                   |
| 5.6  | 生成公网预览 URL         | viteReady 后 → 构造 gateway URL → 浏览器可访问                                                                                                                                                              |
| 5.7  | 预览面板 iframe 加载     | 左边面板 → 自动加载 URL → 看到页面                                                                                                                                                                          |
| 5.8  | 代码修改后热更新         | agent 改了组件 → Vite HMR → 预览自动刷新                                                                                                                                                                    |
| 5.11 | 预览 URL 可分享          | 复制链接 → 其他人能打开                                                                                                                                                                                     |
| 5.12 | Vite 配置正确            | host=0.0.0.0, allowedHosts=true → 外部可访问                                                                                                                                                                |
| 5.13 | 非 coding 模式不启动预览 | 普通对话 → 无 preview → 不浪费资源                                                                                                                                                                          |

---

## 六、云开发工具调用

| #   | 场景                       | 验证内容                                |
| --- | -------------------------- | --------------------------------------- |
| 6.1 | 操作云数据库               | "往 users 插一条" → 数据库有新记录      |
| 6.2 | 操作云存储                 | "上传图片" → 文件出现在云存储           |
| 6.3 | 管理云函数                 | "部署函数" → 函数上线                   |
| 6.4 | 凭证过期自动刷新           | 长对话 → 凭证过期 → 自动刷新 → 继续执行 |
| 6.5 | 云工具不可用不影响文件操作 | MCP 发现失败 → agent 仍能读写文件       |

---

## 七、代码持久化

| #   | 场景                 | 验证内容                                                                    |
| --- | -------------------- | --------------------------------------------------------------------------- |
| 7.1 | 每轮对话后自动归档   | agent 完成 → 工作区 push 到归档仓库 -> 如果node_modules 的话， 打了个压缩包 |
| 7.2 | 归档有版本区分       | 归档仓库 → 每轮对话一个 commit                                              |
| 7.3 | 推送到用户仓库       | 配置个人仓库 → 完成后代码出现在用户 repo                                    |
| 7.4 | 删除 task 时清理归档 | 删除 → 归档仓库对应分支/目录消失                                            |

---

## 九、异常 & 降级

| #   | 场景           | 验证内容                                 |
| --- | -------------- | ---------------------------------------- |
| 9.1 | 无沙箱镜像配置 | 不创建沙箱，agent 受限模式（仅文字）     |
| 9.2 | 沙箱创建失败   | 用户收到错误提示，对话不阻塞             |
| 9.3 | 健康检查超时   | 降级为受限模式，用户看到超时提示         |
| 9.4 | 网络中断后恢复 | 前端自动重连 SSE → 继续看到输出          |
| 9.5 | 启动阶段有进度 | 冷启动时："创建沙箱" → "启动中" → "就绪" |

---

## 十、管理 & 配置

| #    | 场景                  | 验证内容                               |
| ---- | --------------------- | -------------------------------------- |
| 10.1 | 查看活跃沙箱列表      | 管理后台 → 看到所有运行中的沙箱        |
| 10.2 | 隔离模式可切换        | 环境变量/后台配置 shared ↔ isolated    |
| 10.3 | VITE_DEV_OVERLAY 控制 | 设为 false → Vite 全屏错误遮罩关闭     |
| 10.4 | 调试日志按需开启      | AGENT_DEBUG_JSONL=1 → 详细消息日志落盘 |

---

