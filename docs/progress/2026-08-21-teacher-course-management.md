# 教师端课程管理进度日志

## 当前状态

- 工作分支：`codex/teacher-course-management`
- 基线提交：`72b32fd docs(web): plan teacher course management`
- 当前功能提交：`989dacc test(web): guard teacher course routes`
- 实施计划中的 5 轮功能已经完成，当前处于验收与集成阶段。

## 已完成轮次

### 第 1 轮：课程领域模型与查询仓库

- 提交：`767927a feat(web): add teacher course repository`
- 完成课程、章节、课时、资源和 AI 配置类型。
- 添加两套完整演示课程数据。
- 实现课程筛选、指标、详情、课时查询、班级分配和开课输入构造。
- 补充指标断言：`9727e5d test(web): strengthen teacher course metrics assertion`。

### 第 2 轮：课程中心与筛选

- 提交：`e0d66dc feat(web): add teacher course center`
- 完成课程指标、搜索、学段/主题/状态筛选、课程卡片和空状态。

### 第 3 轮：课程详情与班级分配

- 提交：`7723fb0 feat(web): add teacher course detail`
- 完成课程目标、章节课时、班级分配及教师上下文写操作。
- 修复课程切换时的局部状态重置：`2f7d9ad fix(web): reset teacher course detail state`。
- 增加 Node 18 测试兼容处理：`7532a1e fix(web): support Node 18 in course detail tests`。

### 第 4 轮：课时备课与开课联动

- 提交：`48ee9d9 feat(web): add teacher lesson preparation`
- 完成教学目标、课堂步骤、教师提示、资源、作业、Skills、MCP 与开课入口。
- 加强备课页覆盖：`93d9b51 test(web): strengthen teacher lesson preparation coverage`。

### 第 5 轮：路由注册与整体收尾

- 提交：`2b2bb58 feat(web): complete teacher course management`
- 注册课程中心、课程详情和课时备课路由，并移除课程占位页。
- 添加课程路由保护测试：`989dacc test(web): guard teacher course routes`。

## 2026-08-21 验收轮次

- `pnpm.cmd --filter @ai-xiaobao/web test`：通过，24 个测试文件、57 项测试，0 失败。
- `pnpm.cmd type-check`：通过。
- `pnpm.cmd lint --ignore-pattern ".worktrees/**" --ignore-pattern "**/dist/**"`：通过。
- `pnpm.cmd --filter @ai-xiaobao/web build`：通过，Vite 构建 7819 个模块。
- 已知非阻塞提示：测试存在既有 Jotai `atomFamily` 弃用提示；构建存在大于 500 kB 的 chunk 提示。
- Windows 工作区存在大量 LF/CRLF 状态噪音，检查实际变更和暂存范围时需按明确文件路径操作，避免误提交。

## 下一步

1. 当前仓库没有配置 Git 远程地址，暂时无法推送或创建 Pull Request。
2. 已选择保留 `codex/teacher-course-management` 分支及其隔离工作树，不直接改动 `master`。
3. 后续配置远程后可推送并创建 PR；若选择本地合并，应先妥善保存主工作区的主题改动。
4. 集成时必须保留主工作区中 `packages/web/src/index.css` 与 `packages/web/src/main.tsx` 的用户主题改动。

## 2026-08-21 集成决策

- 首选方案：推送分支并创建 Pull Request，避免干扰主工作区的未提交修改。
- 检查结果：`git remote -v` 无输出，仓库尚未配置远程地址。
- 最终处理：保留功能分支与工作树，等待远程地址或主工作区改动得到妥善处理后再集成。

## 2026-08-21 浏览器验收轮次

- 重新运行完整 Web 测试：24 个测试文件、57 项测试全部通过。
- 重新运行正式 Web 构建：通过，共转换 7819 个模块；仅有既有的大包体积提示。
- 使用正式构建产物和本地演示教师会话打开 `/#/teacher/courses`，确认课程中心布局、封面、指标和筛选控件显示正常。
- 实际测试搜索无结果与“清除筛选”，课程列表可正确隐藏并恢复。
- 实际打开“AI 太空海报创作营”详情，勾选第二个班级并保存，成功显示“本次演示配置已保存”。
- 实际进入“让太空校园海报动起来”备课页，教学目标、课堂步骤、教师提示、资源、AI 能力、Skills 和 MCP 均显示正常。
- 实际打开开课确认弹窗并确认开始上课，成功进入活动课堂；课堂学生、进度、额度和 AI 能力显示正常。
- 浏览器验收期间产品页面无错误或警告。临时预览服务和临时脚本已关闭并删除，未产生产品代码改动。

## 2026-08-21 课程封面完整显示修正

- 针对小宝角色封面被裁切的问题，先添加回归测试并确认旧实现会失败。
- 课程卡片封面由裁切显示改为完整适配，加入 16px 安全边距、柔和背景和响应式高度。
- 聚焦测试通过：3 项测试全部通过；完整 Web 测试通过：24 个测试文件、58 项测试，0 失败。
- `pnpm.cmd type-check`、`pnpm.cmd lint --ignore-pattern ".worktrees/**" --ignore-pattern "**/dist/**"` 和正式 Web 构建均通过。
- 使用正式构建产物在内置浏览器打开课程中心验收：两张 1254×1254 原图均以 `contain` 显示在 303×192 区域内，小宝全身完整可见。

## 2026-08-21 作业与作品管理

- 将“作业与作品”从建设占位页升级为可操作的作品管理页。
- 新增作品总数、待点评、已点评和优秀作品指标，以及关键词、班级和点评状态筛选。
- 新增完整作品预览、教师点评与修改、表现等级和优秀作品推荐；作品预览统一完整适配，不裁切小宝角色。
- 教学首页的“全部作品”和待点评入口已接入作品管理页。
- 测试先行：新增测试先因仓库、页面和路由不存在而失败，完成实现后 26 个测试文件、62 项测试全部通过。
- `pnpm.cmd type-check`、代码规范检查和正式 Web 构建均通过；构建转换 7821 个模块，仅有既有的大包提示。
- 内置浏览器实测关键词筛选、点评保存、指标联动、优秀作品推荐及教学首页入口均正常。

## 2026-08-21 学生管理

- 将“学生管理”从建设占位页升级为跨班级学生总览，并复用班级详情中的学生数据。
- 新增学生总数、创作中、本周完成和需要关注指标，以及姓名、班级和学习状态筛选。
- 新增学习概况弹窗，展示任务完成度、最近活跃时间和近期作品。
- 支持将学生标记为“需要关注”或恢复为“创作中”，列表、弹窗和指标即时联动。
- 测试先行：新增测试先因仓库、页面和路由不存在而失败，完成实现后 28 个测试文件、66 项测试全部通过。
- `pnpm.cmd type-check`、代码规范检查和正式 Web 构建均通过；构建转换 7823 个模块，仅有既有的大包提示。
- 内置浏览器实测姓名筛选、学习概况、近期作品及关注状态切换均正常。

## 2026-08-21 课堂记录

- 将最后一个“课堂记录”占位页升级为真实课堂记录总览，教师后台导航不再存在建设占位页面。
- 新增课堂次数、累计课时、平均时长和覆盖学生指标，以及课节/班级搜索与班级筛选。
- 新增课堂回顾弹窗，展示时间、参与人数、安全额度、AI 能力、Skills 和 MCP 服务。
- 扩展结束课堂的数据保存逻辑，新记录会自动保留班级、课节、参与人数及完整课堂配置。
- 教学首页“最近课堂记录”已接入全部记录页面。
- 测试先行：主功能测试先因仓库、页面和路由不存在而失败；浏览器发现弹窗文字对比度问题后再次补充失败测试并修正。
- 最终完整测试通过：30 个测试文件、70 项测试；`pnpm.cmd type-check`、代码规范检查和正式 Web 构建均通过，构建转换 7825 个模块。
- 内置浏览器实测搜索筛选、课堂回顾、教学首页入口和浅色卡片文字对比度均正常。

## 2026-08-21 教学首页入口收尾

- 将无对应页面的“查看教学日历”改为“查看今日安排”，点击后平滑定位到当天课程区域。
- “查看课前准备”已接入当天第一节课的备课页面；“查看全部课程”已接入课程中心。
- 新增回归测试，旧按钮先按预期失败，完成接线后完整测试通过：30 个测试文件、71 项测试。
- `pnpm.cmd type-check`、代码规范检查和正式 Web 构建均通过；构建转换 7825 个模块，仅有既有的大包提示。
- 内置浏览器逐项点击验证今日安排定位、第一节课备课路由和课程中心路由均正常。

## 2026-08-21 进行中课堂操作收尾

- 接通“查看全部学生状态”和“查看需要帮助的学生”，自动携带当前班级与关注状态筛选。
- 新增课堂设置窗口，支持课中调整可选 AI 能力和每人点数额度；AI 对话保持必选，额度限制为 10–500 点，Skills 与 MCP 保持开课前配置。
- 设置保存后立即更新活动课堂，结束课堂时会继续沿用最终能力与额度写入课堂记录。
- 测试先行：新增状态更新、课堂入口和 URL 初始筛选覆盖；完整 Web 测试通过，30 个测试文件、73 项测试。
- `pnpm type-check`、`pnpm lint` 和正式 Web 构建均通过；构建转换 7826 个模块，仅有既有的大包提示。
- 内置浏览器从教学首页真实开课，实测视频创作与 120 点在计时刷新后同时保存；需帮助入口仅显示当前班级的王一诺，全部状态入口正确预选二年级创作 A 班。
- 浏览器实测曾发现设置草稿会被课堂计时刷新重置，已缩小同步依赖并用最新正式构建复验通过。

## 2026-08-21 学生创作搜索

- 将学生编程工具左侧原本只有外观的“搜索创作”接入真实任务列表。
- 支持按作品标题与创作内容即时匹配，自动忽略首尾空格和英文大小写。
- 搜索时数量切换为结果数量，无匹配时展示包含关键词的明确提示；清空后恢复最近创作列表。
- 测试先行：新增测试先因筛选函数不存在和输入框不受控而失败，完成实现后完整 Web 测试通过：30 个测试文件、75 项测试。
- `pnpm type-check`、`pnpm lint` 和正式 Web 构建均通过；构建转换 7826 个模块，仅有既有的大包提示。
- 内置浏览器在学生编程工具输入“太空”验证结果计数与空状态，并清空关键词确认恢复最近创作状态。

## 2026-08-21 六大创作入口大师 Skills 设计

- 确定为绘画、视频、音乐、游戏、写作和学习辅导分别提供一个内置大师 Skill，并由学生入口显式绑定。
- 确定统一的需求理解、专业策划、工具执行、质量检查、反馈修改、成果交付和学习复盘流程。
- 视频大师 Skill 将角色参考图、故事连续性表和九宫格分镜设为不可跳过的质量门禁；角色参考与九宫格需要学生确认。
- 确认视频后续接入火山引擎；音乐先采用可替换工具契约，未接入真实服务前不得宣称已生成音频。
- 正式设计保存至 `docs/superpowers/specs/2026-08-21-student-master-skills-design.md`，等待文档确认后编写实施计划。

## 2026-08-21 六大创作入口大师 Skills 实施计划

- 已将确认后的设计拆成 7 个可测试任务：质量契约、三项创作 Skill、游戏/写作/学习 Skill、入口映射、任务自动注入、学生流程卡和全量验收。
- 明确本轮交付六个大师 Skills、六入口显式绑定和学生可见七阶段流程；视频与音乐工具尚未接入时必须如实显示状态，不得伪造成品。
- 视频实施条目已固定角色设定锁、多角度参考图、故事连续性表、多页九宫格、两次学生确认、逐镜生成、粗剪检查和局部返工等硬门禁。
- 正式实施计划保存至 `docs/superpowers/plans/2026-08-21-student-master-skills.md`；下一步按测试先行方式执行 Task 1。

## 2026-08-21 绘画大师 Skill

- 建立六个大师 Skill 的真实 Loader 回归测试；红灯确认四个 Skill 缺失、原游戏与写作 Skill 缺少完整七阶段。
- 新增 `student-image-master`，覆盖少量关键提问、视觉策划、真实图片生成/编辑、查看成图、质量体检、局部修改、交付和学习复盘。
- 明确图片工具未返回时不得宣称完成，也不得使用文字、虚假链接或占位图冒充作品。
- 绘画大师定向测试 2 项通过，官方 Skill 结构校验通过；独立行为验证确认补齐基线遗漏的视觉方案、真实图片查看门禁和学习复盘。

## 2026-08-21 视频大师 Skill

- 新增 `student-video-master`，将角色一致性和故事连续性写成不可跳过的顺序门禁。
- 角色设定锁、多角度/多表情参考、故事连续性表、严格 3×3 九宫格、两次学生确认、逐镜继承、粗剪检查与局部返工均已纳入完整七阶段。
- 明确学生即使要求“直接生成”也不跳过两个关键确认点；其余专业步骤由 Skill 自动完成，学生无需接触技术配置。
- 火山引擎工具尚未接入时只交付真实中间成果并标记“尚未生成视频”，禁止用图片轮播、占位文件或假链接冒充成片。
- 视频大师定向测试 3 项通过，官方 Skill 结构校验通过；独立行为验证确认补齐普通助手基线遗漏的八项视频门禁。

## 2026-08-21 音乐大师 Skill

- 新增 `student-music-master`，由学生的简单要求自动形成曲式时间轴、速度/拍号/调性、配器、人声、歌词和生成约束。
- 将音乐策划与真实音频分开验收；只有真实可播放工具结果经过完整听检后才能称为音乐成品。
- 音乐供应商尚未选定时明确显示“尚未生成音频”，保留制作蓝图与可复用参数，学生不需要配置密钥或理解供应商。
- 增加按具体时间段和音乐层定位问题的局部修改流程，保护已通过的旋律、歌词和结构。
- 音乐大师定向测试 3 项通过，官方 Skill 结构校验通过；独立行为验证确认补齐基线遗漏的曲式参数、听检、局部修改、交付和学习复盘。

## 2026-08-21 游戏大师 Skill

- 将原 `scratch-game-coach` 从简短教学提示升级为完整七阶段游戏大师，不改变快捷入口后续绑定所用名称。
- 固定“先最小可玩版本、真实启动试玩、核心通过后再加装饰”的顺序，学生无需选择技术栈或配置环境。
- 玩法体检覆盖启动、操作、边界、碰撞、计分、胜负、重开和快速输入等异常路径；未实际运行不得宣称完成。
- 修改流程按具体规则或对象局部返工，并在每次修改后重跑相关检查和至少完整玩一局。
- 游戏大师定向测试 2 项通过，官方 Skill 结构校验通过；独立行为验证确认补齐基线遗漏的真实运行门禁和完整试玩闭环。

## 2026-08-21 写作大师 Skill

- 将原 `student-writing-coach` 升级为完整七阶段写作大师，保持学生快捷入口所需的稳定名称。
- 明确作业与待提交作品只提供关键提问、结构、句子支架、逐段反馈和局部润色，不输出可直接提交的成人代写全文。
- 写作过程保留学生观点、真实经历和有特点的措辞；事实不确定时标记待核实，不编造经历冒充孩子本人。
- 作品体检覆盖主题、结构、逻辑、语言、事实、儿童声音、原创参与和适龄安全，并把每轮改进限制在最重要的 1–3 项。
- 写作大师定向测试 2 项通过，官方 Skill 结构校验通过；独立行为验证确认原本会完整代写的基线已转为 3 个关键问题和学生参与式闭环。

## 2026-08-21 学习大师 Skill 与六 Skill 整体验收

- 新增 `student-learning-master`，通过诊断—单步提示—等待回答—理解检查—针对纠错—小变式—复盘完成学习闭环。
- 即使学生要求“直接告诉答案”，Skill 也只缩短说明，不跳过关键作答；作业、测验和竞赛场景不代做整份答案。
- 学习大师定向测试 2 项与官方 Skill 结构校验通过；独立行为验证确认普通助手直接给完整答案的基线已转为单个诊断问题并等待学生回答。
- 六个大师 Skills 现均可被生产 Skill Loader 解析，整体契约测试 14 项全部通过。

## 2026-08-21 快捷入口自动绑定大师 Skill

- 六个学生快捷入口已显式绑定唯一内置大师 Skill，映射成为前端唯一事实来源。
- 点击入口时会同时填入适龄起始请求并保存对应大师 Skill；学生无需安装、勾选或进入高级设置。
- 创建学生任务时，内置大师 Skill 会自动与已有手选 Skills 去重合并到现有 `skillList`；普通非学生任务保持原行为。
- 视频入口如实标记火山引擎待接入，音乐入口如实标记供应商待选择，其余四项标记工具可用。
- 自动映射、工作区回调和任务 Skill 合并共 10 项聚焦测试全部通过。

## 2026-08-21 学生可见大师流程卡

- 学生选择快捷入口后会立即看到对应大师名称、能力说明和“理解想法—制定方案—开始创作—作品体检—认真修改—交付作品—学习复盘”七阶段。
- 视频流程卡额外展示“角色参考图”和“九宫格分镜”两个学生确认点，以及火山引擎待接入的真实状态；音乐卡显示服务待选择，不误报音频已生成。
- 流程卡与入口切换保持同步，学生不需要安装、勾选或配置 Skill。
- 流程卡与自动绑定聚焦测试 13 项通过，项目类型检查与正式 Web 构建通过；构建转换 7828 个模块，仅有既有的大包提示。
- 内置浏览器逐项点击六个入口，确认大师名称、七阶段、视频确认点、工具状态和起始请求均同步变化；页面实际布局完整清楚。
- 静态预览未连接后端，任务轮询产生既有 JSON 解析错误；本轮大师流程卡交互本身未发现运行错误，真实提交留待连接后端的环境验收。
- 完整 Web 测试通过：32 个测试文件、83 项测试；代码规范检查无新增输出。

## 2026-08-21 CodeBuddy 模型目录更新

- 核对 DeepSeek 官方资料：官方最新一代已是 V4，提供 `deepseek-v4-flash` 与 `deepseek-v4-pro`。
- 使用现有服务端账号只读查询 CodeBuddy SDK 实际模型目录，确认该渠道目前仍只开放 `deepseek-v3-2-volc / DeepSeek-V3.2`，因此未进行会导致调用失败的假升级。
- 将 CodeBuddy 模型发现改为默认实时读取账号目录；读取失败时仍安全回退到内置目录，也可用 `CODEBUDDY_DYNAMIC_MODELS=0` 显式关闭。
- 前端与回退目录将 V3.2 标记为“CodeBuddy 当前可用”，避免与 DeepSeek 官方最新版混淆；渠道未来开放 V4 后可自动出现在列表中。
- 新增动态模型发现测试 3 项，并与大师 Skills 测试合计 17 项全部通过；项目类型检查通过。
